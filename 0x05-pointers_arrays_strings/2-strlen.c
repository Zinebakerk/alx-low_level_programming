#include "main.h"
/**
 * _strlen - function that return the length of a string
 * @s: char data tyype
 * Return: the length of a string
 */
int _strlen(char *s)
{
long int i;
for (i = 0; i < s[i]; i++)
{
}
return (i);
}
