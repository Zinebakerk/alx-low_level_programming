#include <unistd.h>
/**
 * _putchar - function that writes the character c to stdout
 * @c: The character to print
 * Return: 1 if the run was succesufily done
 * On error : -1 should be returned
 */
int _putchar(char c)
{
return (write(1, &c, 1));
}
